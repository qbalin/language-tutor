# Python
.venv/
__pycache__/
*.pyc

# Language data (large downloads/DBs) and student progress — personal, never shared
languages/*
!languages/.gitkeep
# ... but per-book apparatus skip lists are shareable config, not student data
!languages/*/
languages/*/*
!languages/*/grammar_skip.txt

# Progress checkpoints — the student's own git repo with a PRIVATE remote
progress/

# Local overrides
.claude/settings.local.json
.DS_Store

# Alias for preferred model
tutor.sh
